var React = require("react");

function TimelineView(props) {
    return (
      <div id="main">
      <h1> Bienvenue sur Messy </h1>
      <br/>
      <label>Disconnect</label>
        <input
           name="disconnect"
           value={"disconnect"}
           type = "button"
         />
      </form>
      </div>
    );
}

module.exports = TimelineView;
